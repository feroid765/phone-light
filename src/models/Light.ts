import {Color} from 'react-color';

class Light{
    Name: string;
    Color: Color;
    
    constructor(
        Name: string,
        Color: Color,
    ){this.Name=Name;this.Color=Color;}
}

export default Light;